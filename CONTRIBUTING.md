# Руководство по контрибьюции

Благодарим за интерес к проекту Twitch AI Bot! Это руководство поможет вам внести свой вклад в развитие проекта.

## Содержание

- [Кодекс поведения](#кодекс-поведения)
- [Как начать](#как-начать)
- [Структура проекта](#структура-проекта)
- [Процесс разработки](#процесс-разработки)
- [Стандарты кода](#стандарты-кода)
- [Тестирование](#тестирование)
- [Создание Pull Request](#создание-pull-request)
- [Сообщения о багах](#сообщения-о-багах)
- [Запросы новых функций](#запросы-новых-функций)

## Кодекс поведения

Мы стремимся создать дружелюбное и инклюзивное сообщество. Все участники должны следовать нашему [Кодексу поведения](CODE_OF_CONDUCT.md).

## Как начать

### Предварительные требования

- Node.js 18+ и npm 8+
- Docker и Docker Compose
- Git
- Текстовый редактор с поддержкой TypeScript (рекомендуется VS Code)

### Установка

1. **Форкните репозиторий**
   ```bash
   git clone https://github.com/your-username/twitch-ai-bot.git
   cd twitch-ai-bot
   ```

2. **Установите зависимости**
   ```bash
   npm install
   ```

3. **Настройте переменные окружения**
   ```bash
   cp .env.example .env
   # Отредактируйте .env файл со своими настройками
   ```

4. **Запустите базу данных**
   ```bash
   docker-compose up -d postgres redis
   ```

5. **Соберите проект**
   ```bash
   npm run build
   ```

6. **Запустите в режиме разработки**
   ```bash
   npm run dev
   ```

## Структура проекта

```
twitch-ai-bot/
├── src/
│   ├── config/          # Конфигурация приложения
│   ├── services/        # Бизнес-логика и API сервисы
│   ├── types/           # TypeScript типы и интерфейсы
│   ├── utils/           # Утилиты и вспомогательные функции
│   └── index.ts         # Точка входа
├── tests/               # Тесты
├── docs/                # Документация
├── docker-compose.yml   # Docker конфигурация
├── package.json         # Зависимости и скрипты
├── tsconfig.json        # Конфигурация TypeScript
└── README.md           # Основная документация
```

## Процесс разработки

### Git Workflow

1. **Создайте ветку для новой функции**
   ```bash
   git checkout -b feature/your-feature-name
   # или
   git checkout -b fix/your-bug-fix
   ```

2. **Внесите изменения** и закоммитьте их
   ```bash
   git add .
   git commit -m "feat: добавлена новая функция X"
   ```

3. **Отправьте изменения в ваш форк**
   ```bash
   git push origin feature/your-feature-name
   ```

4. **Создайте Pull Request**

### Типы веток

- `feature/` - новые функции
- `fix/` - исправления багов
- `docs/` - обновления документации
- `refactor/` - рефакторинг кода
- `test/` - добавление тестов

## Стандарты кода

### TypeScript

- Используйте строгую типизацию
- Предпочитайте интерфейсы типам
- Избегайте `any` без необходимости
- Используйте meaningful names для переменных и функций

### Форматирование

Мы используем Prettier для форматирования кода:

```bash
npm run format
```

### Linting

```bash
npm run lint
npm run lint:fix  # Автоматическое исправление
```

### Комментарии к коммитам

Мы следуем [Conventional Commits](https://www.conventionalcommits.org/):

- `feat:` - новая функция
- `fix:` - исправление бага
- `docs:` - изменения в документации
- `style:` - форматирование кода
- `refactor:` - рефакторинг
- `test:` - добавление тестов
- `chore:` - обновление зависимостей

Примеры:
```bash
git commit -m "feat: добавлена система команд"
git commit -m "fix: исправлена ошибка в модуле авторизации"
git commit -m "docs: обновлена документация по установке"
```

## Тестирование

### Запуск тестов

```bash
npm test                    # Все тесты
npm run test:watch         # Тесты в режиме наблюдения
npm run test:coverage      # С покрытием кода
```

### Написание тестов

- Используйте Jest для тестирования
- Тестируйте бизнес-логику, а не детали реализации
- Используйте meaningful test descriptions
- Стремитесь к 80%+ покрытию кода

### Пример теста

```typescript
describe('CommandService', () => {
  it('должен обрабатывать команду корректно', async () => {
    const result = await commandService.executeCommand('!test', user);
    expect(result).toBeDefined();
  });
});
```

## Создание Pull Request

### Чек-лист перед PR

- [ ] Код следует стандартам проекта
- [ ] Все тесты проходят
- [ ] Добавлены тесты для новой функциональности
- [ ] Обновлена документация
- [ ] Нетconsole.log в продакшн коде
- [ ] Произведено linting и форматирование

### Описание PR

Опишите:

1. **Что было изменено**
2. **Почему эти изменения необходимы**
3. **Как это было протестировано**
4. **Скриншоты (если применимо)**

Пример:

```markdown
## Описание

Добавлена система логирования для отслеживания команд пользователей.

## Изменения

- Создан `LoggingService` для централизованного логирования
- Добавлены логи в `CommandService`
- Обновлен `package.json` с новой зависимостью

## Тестирование

- Написаны unit тесты для `LoggingService`
- Протестировано в dev окружении
- Проверено отсутствие performance impact
```

## Сообщения о багах

Если вы нашли баг, пожалуйста, создайте Issue со следующей информацией:

### Шаблон бага

```markdown
**Описание бага**
Краткое описание бага.

**Шаги для воспроизведения**
1. Перейдите к '...'
2. Нажмите на '....'
3. Прокрутите до '....'
4. Увидите ошибку

**Ожидаемое поведение**
Описание того, что вы ожидали.

**Скриншоты**
Если применимо, добавьте скриншоты.

**Окружение**
- OS: [например, iOS]
- Browser [например, chrome, safari]
- Version [например, 22]
- Node.js Version: [например, 18.0.0]

**Дополнительная информация**
Любая другая информация о проблеме.
```

## Запросы новых функций

### Шаблон запроса функции

```markdown
**Описание проблемы**
Описание проблемы, которую решает данная функция.

**Предлагаемое решение**
Описание того, что вы хотели бы видеть.

**Альтернативные решения**
Описание любых альтернативных решений.

**Дополнительная информация**
Любая другая информация о запросе функции.
```

## Дополнительная помощь

Если у вас есть вопросы:

1. Проверьте [документацию](README.md)
2. Поищите в [Issues](https://github.com/svatowl-tech/twitch-ai-bot/issues)
3. Создайте новое Issue с меткой `question`

Спасибо за ваш вклад! 🎉